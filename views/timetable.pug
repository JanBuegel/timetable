doctype html
html
  head
    title Open Flair Festival 2024
    link(rel='stylesheet', href='/css/styles.css')
    link(rel='stylesheet', href='https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200')
    script(src='/js/main.js')
  body
    h1 Open Flair 2024
    .day-buttons
      button.day-button(data-day='Mittwoch')
        span.material-symbols-outlined calendar_today
        p Mittwoch
      button.day-button(data-day='Donnerstag')
        span.material-symbols-outlined calendar_today
        p Donnerstag
      button.day-button(data-day='Freitag')
        span.material-symbols-outlined calendar_today
        p Freitag
      button.day-button(data-day='Samstag')
        span.material-symbols-outlined calendar_today
        p Samstag
      button.day-button(data-day='Sonntag')
        span.material-symbols-outlined calendar_today
        p Sonntag
    .container
      each date in Object.keys(eventsGroupedByDate)
        .event-group(data-day=date)
          .event-group-header= date
          each event in eventsGroupedByDate[date]
            .event
              .details
                h2= event.name
                p= event.stage
                p= event.time
    // Add the filter button
    .filter-button
      span.material-symbols-outlined filter_list

    // Add the search button
    .search-button
      span.material-symbols-outlined search

    // Add the reset button (initially hidden)
    .reset-button.hidden
      span.material-symbols-outlined restart_alt

    // Add the modal for the stage filter
    .modal.hidden
      .modal-content
        span.close-button &times;
        h2 Select Stage
        ul.stage-list
